import { createContext } from "react";

export const UserLocationContext = createContext<any>(null);

export const FromAddCoordsContext = createContext<any>(null);

export const ToAddCoordsContext = createContext<any>(null);

export const DirectionDataContext = createContext<any>(null);

export const DistanceDurationContext = createContext<any>(null);

export const PayingAmountContext = createContext<any>(null);